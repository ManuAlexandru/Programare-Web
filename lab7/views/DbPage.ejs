<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,minimum-scale=1">
    <title>DbPage</title>
    <!-- the form awesome library is used to add icons to our form -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css">
    <!-- include the stylesheet file -->
    <link href="/style.css" rel="stylesheet" type="text/css">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js">  </script>
    <script>


        $(document).ready(function () {
            var varsta, animal;
            $("#submit").click(function () {
                animal = $("#Animal").val();//preiau animalul ul introdus in campul cu id ul username dupa apasarea butonului

                varsta = $("#Varsta").val();//preiau varsta acestuia introdus in campul cu id ul password dupa apasarea butonului
                console.log(animal);
                console.log(varsta);
                $.post("http://localhost:5000/DbPage", { animal: animal, varsta: varsta }, function (data) {
                    console.log(data);
                });
            });
        });

    </script>
</head>

<body>
    <div class="login">
        <h1>Login</h1>
        <div>
            <label for="Animal">
                Specia

            </label>
            <input type="text" name="Animal" id="Animal" required>
            <label for="Parola">
                Varsta
            </label>
            <input type="Varsta" name="Varsta" id="Varsta" required>
            <button type="button" id="submit"> Introduceti Datele</button>
        </div>
        <a href="/">back</a>
    </div>

</body>

</html>